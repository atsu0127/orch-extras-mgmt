# 練習の一括作成 — タスク

設計は [design.md](./design.md)。横断制約は [platform/design.md](../platform/design.md)。サイズは相対見積り（S: 1時間以内 / M: 半日 / L: 1日以上）。

## 進捗

- **T3 完了**（2026-08-01）— `/admin/practices` に一括追加 UI（会場の既存／新規）を追加
- 次に着手できるもの: **T4**（E2E・全検査）

## 進め方

```text
T1（設計・ADR）→ T2（入力検証・mutation）→ T3（管理UI）→ T4（E2E・文書・検査）
```

## タスク

| ID | 内容 | 完了条件 | 依存 | サイズ |
| --- | --- | --- | --- | --- |
| T1 | `practice-bulk-create` の design/tasks を整え、CS 同期ディレクトリを除去。ADR と platform 索引・README を更新 | 機能一覧が一括作成を指し、旧 CS 同期の生きた正が残っていない | — | S |
| T2 | 一括作成の zod・mutation（会場新規／既存／同名再利用、30件上限） | 単体テストで追加・上限・同名再利用・住所食い違いエラーが通る | T1 | M |
| T3 | `/admin/practices` の一括追加 UI（会場の選択／新規） | 管理者が複数行を送信でき、演奏会切り替えで入力が消える。回帰テストあり | T2 | M |
| T4 | E2E、進捗・README、全検査 | 管理者の一括追加が E2E で確認でき、lint / typecheck / test が通る | T3 | S |

## 受け入れ条件

- [ ] 管理者が練習を一括追加できる
- [ ] 会場は既存選択と新規登録（同名は再利用）ができる
- [ ] 1回 30件超は拒否される
- [ ] 演奏会切り替えで一括フォームの入力が残らない
- [ ] extra は実行できない
- [ ] サークルスクエア連携・Cron を追加していない

## 実装時の注意

- 練習は新規 INSERT のみ。既存の更新・削除はしない
- 会場マスタの更新・削除はしない（未登録名の追加と、同名の再利用のみ）
- クエリは行数に比例して増やさない
- 画面から drizzle スキーマを直接 import しない
