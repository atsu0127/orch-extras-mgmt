# 練習の一括作成 — タスク

設計は [design.md](./design.md)。横断制約は [platform/design.md](../platform/design.md)。サイズは相対見積り（S: 小さな差分 / M: 中程度 / L: 広い変更）。

## 進捗

- **完了**（2026-08-01）— 初期 T1〜T4 および UI 改修 T5〜T7
- 次に着手できるもの: なし（本機能は完了）

## 進め方

```text
（初期）T1 → T2 → T3 → T4（完了）
（改修）T5（mutation 簡素化）→ T6（トグル・複製・会場 modal UI）→ T7（E2E・文書・検査）
```

## タスク

| ID | 内容 | 完了条件 | 依存 | サイズ |
| --- | --- | --- | --- | --- |
| T1 | `practice-bulk-create` の design/tasks を整え、CS 同期ディレクトリを除去。ADR と platform 索引・README を更新 | 機能一覧が一括作成を指し、旧 CS 同期の生きた正が残っていない | — | S |
| T2 | 一括作成の zod・mutation（会場新規／既存／同名再利用、30件上限） | 単体テストで追加・上限・同名再利用・住所食い違いエラーが通る | T1 | M |
| T3 | `/admin/practices` の一括追加 UI（会場の選択／新規） | 管理者が複数行を送信でき、演奏会切り替えで入力が消える。回帰テストあり | T2 | M |
| T4 | E2E、進捗・README、全検査 | 管理者の一括追加が E2E で確認でき、lint / typecheck / test が通る | T3 | S |
| T5 | 一括入力から会場新規を外し、`venueId` のみにする。単体テストを更新 | 一括 mutation が会場 INSERT せず、既存 ID／未設定だけで通る | T4 | M |
| T6 | 一括 UI をトグル化。「行を追加」「この行を複製」、会場 modal 即保存＋一覧反映 | 閉じても下書き保持、複製できる、modal 保存後にセレクトへ出る。演奏会切替で消える回帰あり | T5 | M |
| T7 | E2E・設計／進捗・全検査 | 改修後の一括・会場 modal が E2E で確認でき、lint / typecheck / test が通る | T6 | S |

## 受け入れ条件

- [x] 管理者が練習を一括追加できる
- [x] 1回 30件超は拒否される
- [x] 演奏会切り替えで一括フォームの入力が残らない
- [x] extra は実行できない
- [x] サークルスクエア連携・Cron を追加していない
- [x] 一括 UI はトグルで、閉じても下書きが残る
- [x] 「行を追加」は空、「この行を複製」でコピーできる
- [x] 会場は modal で即保存し、一括行のセレクトから選べる
- [x] 一括送信時に会場を新規作成しない

## 実装時の注意

- 練習は新規 INSERT のみ。既存の更新・削除はしない
- 一括 mutation では会場マスタを作らない（modal／既存会場 API に任せる）
- クエリは行数に比例して増やさない
- 画面から drizzle スキーマを直接 import しない
